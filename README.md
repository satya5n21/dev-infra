# Dev Infra

This repo contains docker-compose setups for local development databases.

## Databases
- PostgreSQL
- Cassandra
- Redis

## Usage
Each database can be started independently.

Example:
cd postgres
docker compose up -d